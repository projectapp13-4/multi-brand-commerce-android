# Workspace Overview

Date: 2026-07-19
Scope: initial and revised Phase 1 preparation only; no production application scaffold or feature implementation.

## Directory map

| Directory | Initial files / bytes | Role | Treatment |
|---|---:|---|---|
| `../gurbakir` | 6,537 files; 3,386,175,498 bytes (3.154 GiB) | Older incomplete Flutter prototype and possible project-owned configuration source | Read-only sunk-cost input; zero platform-selection weight |
| `../the private reference corpus | 113,396 files; 6,679,014,690 bytes (6.220 GiB) | Complete static reconstruction of the third-party reference reference APK | Immutable high-value behavioral/feasibility evidence; never a code donor |
| `.` (`historical implementation workspace`) | Empty at intake | Clean future application workspace | Git repository containing Phase 1 docs/policy/index/validator/evidence skill only |

The roles are intentionally different. No sibling was moved, renamed, or used as a production source tree.

## Inputs

### Old `gurbakir` prototype

- Flutter 3.44.4 / Dart 3.12.2 with Riverpod, `graphql_flutter`, Hive, cached images, shimmer, and URL launch.
- Contains a small catalog/cart proof and legacy legacy prototype brand/default identifiers/assets.
- Its 21 `lib` files were immutability-hashed and deterministic historical checks passed.
- It is not a production architecture, migration base, platform argument, or shared-iOS requirement.

### Reference APK

- Functioning native Android single-activity Jetpack Compose architecture with Hilt/Dagger, coroutines/Flow, Apollo, Retrofit/OkHttp, DataStore, Shopify Checkout Kit, and Firebase.
- Verified counts: 9,573 classes, 43,235 methods, 248,417 call edges, 31 GraphQL operations, 25 destinations, 24 inventoried candidate feature groups pending product acceptance, 5 service endpoints, and 9 storage records.
- Source `APP.apk` and verified working copy are 5,903,061 bytes and SHA-256 `652CA23751A74F67F53409C29B4E347A02C76AB2F870A6824DE3DAD5CE88F602`.
- Its functioning architecture materially informs feasibility and lifecycle concerns; selected libraries are still independently justified from current official sources.

### Future workspace

- Approved Phase 2 direction: new native Android Kotlin + Jetpack Compose application.
- Future iOS may be an independent Swift + SwiftUI product.
- No `settings.gradle(.kts)`, root/module Gradle build, `app/src`, `pubspec.yaml`, `lib/main.dart`, production screen, or feature implementation exists at Phase 1 close.
- Preparation lives under `docs/preparation`; ADRs under `docs/decisions`; evidence navigation under the retired private evidence-navigation helper.
- Root `AGENTS.md` carries durable authority, platform, provenance, secret, iOS, multi-brand, and verification rules.

## Historical prototype validation

The old Flutter prototype was checked without changing the raw sorted source aggregate (`D7E6B75F78E27AF5E724E28C58A9D38686CE32CA1C934A056C0AC5EE8F167644`):

- format: 24 files, 0 changes;
- analyzer: no issues;
- focused widget test: 1 passed;
- Android debug build: passed with an SDK XML-version warning;
- web release build: passed and was visually checked at desktop/mobile sizes.

These results remain valid audit evidence only. They receive zero weight in the native platform decision.

## Revised workstation state

- Android Studio updated to 2026.1.2.
- JDK 17 and Android SDK API 34/35/36, platform/build/emulator tools present.
- Firebase CLI 15.24.0 and Gitleaks 8.30.1 installed and execution-tested.
- CPU virtualization firmware reports disabled and no device was connected; physical-device-first validation is required.
- Two curated security skills installed for the next Codex turn; repo evidence skill validated.

## Git recovery points

- Initial Phase 1: `9c0d9af3c5ab2d81b6fbc3dc563b9adef960d6d1` (`chore: preserve initial Phase 1 assessment`).
- Revised Phase 1: created only after the expanded preparation validator and focused checks pass.
- No remote exists and no push occurred.

## Network-isolation handoff

The prior APK static-analysis isolation's 12 exact outbound rules were removed through its documented rollback; revised validation requires zero enabled matching rules. Re-enable them only through the evidence workspace scripts for an exact future static-analysis session. Normal Android development must not run under that isolation.
