# Pre-P3-16 account deletion route and merchant process

Status (2026-09-26): **public request route, merchant intake and merchant-to-Shopify personal-data-erasure handoff verified on a no-order synthetic case**. Shopify accepted the request and scheduled provider processing for 2026-10-05; final redaction of that case is not yet observed. The scheduled date is case-tracking information, **not a project or P3-16 engineering wait gate**. P-18 remains **PARTIAL — EXTERNAL PROCESS STILL REQUIRED** for production merchant operations and governance described below.

This scoped continuation follows the historical [P3-13 handoff](P3-13-HANDOFF.md). It does not start P3-16 or turn local cleanup into evidence of remote deletion.

## Typed route and trust boundary

The application identity owns `webRoles.legalSupport.origin` and optional `paths.accountDeletionRequest` in the tracked onboarding registry. The narrow role is a relative `/pages/<slug>` path, distinct from Support and Privacy. The registry rejects unsafe path syntax and duplicate roles, then projects the public value into the brand application build. The app's exact-route policy checks the resulting HTTPS URL before Custom Tabs opens it. Shopify content may provide words and a contact form behind that approved route; it cannot select Android navigation, arbitrary URLs, credentials, or executable behavior.

Gürbakır development and staging share one brand-level resource: `https://gurbakir.com/pages/uygulama-hesap-silme-talebi`. A separate profile path was unnecessary because both profiles use the same merchant origin. The published Shopify Page uses the existing `contact` theme template and identifies the app-account and associated-data request. It offers the merchant contact email as a fallback and avoids a deadline or blanket erasure promise. Signed-out GET currently returns HTTP 200 with deletion-specific text and a contact form. An earlier project-controlled form-only rehearsal returned `contact_posted=true`; its message explicitly instructed the merchant not to delete anything. A second, no-order synthetic case also returned `contact_posted=true`. The owner's inbox screenshot shows the second case's matching reference and synthetic alias. Raw screenshots stay outside Git. The owner reports sending an acknowledgement, but its sent copy, wording and delivery were not independently witnessed.

Trial has no deletion-specific merchant resource. Its development-only deletion screen therefore has no request button; it does not inherit Gürbakır's route or treat its generic Support page as a deletion endpoint. Synthetic keeps Customer Account disabled and has no deletion entry. Before any account-enabled brand becomes release-ready, its own public resource and process must pass all three acceptance layers below.

## Merchant operational process

The merchant, not Android, owns intake and completion. A restricted ledger or helpdesk can use the following states:

| State | Required evidence or action |
|---|---|
| Available | Public deletion-specific page and form work signed out. |
| Received | Merchant records an opaque request ID, timestamp and channel after confirming inbox/helpdesk receipt. |
| Verification pending or matched | Match only the necessary account reference; verify identity without asking for password or one-time code. |
| Acknowledged | Send a human acknowledgement through the controlled merchant channel. No invented fixed SLA. |
| Retention review | Record the applicable exception decision and processor checklist without copying customer PII into repository evidence. |
| Provider action pending | Authorized operator uses Shopify Admin's customer personal-data erasure workflow and addresses other relevant processors. Profile deletion is a distinct action with different eligibility. |
| Provider confirmed or partial retention | Read back provider status and recorded exceptions; an accepted mutation is not completion proof. |
| Notified and closed | Tell the requester the supported outcome and close the restricted record. Failures and duplicate requests remain tracked for retry or escalation. |

The minimum restricted record is request ID, timestamps, channel, merchant account reference, verification status, assigned operator, acknowledgement, retention decision reference, processor action/status references, notification and closure state. Keep it out of Git and public reports. A merchant owner must assign the operator and decide any response target. Shopify's ten-day cancellation window for a submitted erasure request is **not** a merchant response SLA.

## Acceptance layers and remaining evidence

1. **Tracked configuration:** the registry, projections, build fields, role mapping, path policy and unit tests must pass. Missing or unsafe deletion paths fail the release-readiness check.
2. **Public resource:** run `pwsh -NoProfile -File scripts/Test-AccountDeletionResource.ps1 -Application <brand> -Profile <profile>` from a signed-out context. This verifies HTTP 200, brand/deletion wording and a form. It does not submit a request.
3. **Merchant process:** use only an unmistakably project-controlled synthetic identity to prove form submission, merchant inbox/helpdesk receipt, identity match, applicable Shopify action and accepted provider handoff/readback. Before production release, assign an accountable operator and establish a restricted case record, processor/retention review, acknowledgement and pending/completion/partial-retention notices, and duplicate/failure handling. Shopify fulfills an accepted erasure asynchronously; final redaction of this one rehearsal case is not required before engineering or release preparation continues. No actual customer should be touched without the owner-policy point-of-action confirmation.

The app screen treats opening and returning from the browser as unverified navigation. Local cleanup remains separately confirmed and has its existing Search, Wishlist, Cart and sign-out choices. Remote erasure cannot be inferred from either action.

## 2026-09-25 merchant operations follow-up

The new case is an unmistakably tagged, project-controlled Shopify customer with zero orders. The owner used Shopify Admin to submit **personal-data erasure** for that exact synthetic profile. The Admin banner schedules erasure for **2026-10-05**. A later profile-deletion attempt was refused because the erasure request is pending; read-only Admin GraphQL still finds the zero-order profile and now reports `canDelete=false`. This proves the **merchant-to-Shopify handoff is operational** and the provider request is accepted and pending; it does not prove redaction, profile removal or case completion. Shopify's [data-request guidance](https://help.shopify.com/en/manual/privacy-and-security/privacy/processing-customer-data-requests) (re-read 2026-09-26) says Shopify processes a submitted erasure request without a second submission or Support contact, provides a ten-day cancellation window, and leaves the merchant responsible for other companies outside installed apps and channels. [Customer management guidance](https://help.shopify.com/en/manual/customers/manage-customers) distinguishes personal-data erasure from profile deletion and explains why pending redaction blocks profile deletion. The provider's scheduled processing date is not an Android test loop or a project wait gate. Do not cancel or repeat the pending request to test retries.

On the Samsung physical device, the owner completed hosted Customer Account sign-in after one generic callback failure and an immediate successful retry. The first failure's cause is unknown. The owner identified the signed-in account as real/personal. Read-only navigation from that account reached the typed Account deletion screen and launched the exact approved public URL in a Custom Tab; returning to the app displayed its explicit “request not verified” message. No request or local cleanup was performed from the real account. The campaign's signed-in **synthetic** account journey remains unrun.

The restricted synthetic ledger is outside Git. A local deterministic duplicate/unknown-result/retry process rehearsal passed nine checks, but live failure handling and a production operator, restricted case-record owner, processor/retention checklist, response target and truthful acknowledgement/pending/completion/partial-retention notices remain release-governance inputs. The owner-reported acknowledgement is not a witnessed sent/delivered message. The synthetic order-bearing fixture was inspected read-only: its one test order makes profile deletion ineligible, and requester control for that historical identity remains unverified. It was not altered. Current operational disposition is **P-18 PARTIAL — EXTERNAL PROCESS STILL REQUIRED** because production operations are not yet assigned and accepted, not because the Shopify handoff failed or its scheduled processing date must pass. A later case-specific redaction readback is useful for merchant follow-through but is not a prerequisite for continuing engineering/release preparation.

Current visitor-consent disposition is **BLOCKED — MERCHANT/LEGAL CONSENT DECISION REQUIRED**. Shopify's Türkiye consent-policy readback says `consentRequired=false` and `dataSaleOptOutRequired=false`; active-market readback showed Türkiye only. Authenticated Gürbakır Admin readback found a configured cookie banner for 31 listed regions, Shopify Network Intelligence enabled, and one Google & YouTube Web app pixel with optimized data access. That pixel's privacy setting says consent is not required and its collection is not classified as data sale. The connected API scope could not read Customer Privacy or server-pixel inventory; the browser supplied only the visible Web-pixel view. Shopify's [browser Customer Privacy API](https://shopify.dev/docs/api/customer-privacy) governs site choices, while [Checkout Kit's mobile guidance](https://shopify.dev/docs/storefronts/mobile/checkout-kit/privacy-compliance) directs apps that collect a choice to pass it through Storefront context; the reviewed contracts do not establish a supported native read/sync of a hosted visitor choice. This last point is a documentation-based inference, not proof that no such integration could ever exist. The technical recommendation for the observed Türkiye-only model is to avoid inventing a second Android prompt and leave `visitorConsent` omitted if merchant/legal owners adopt hosted controls after reviewing pixel, Network Intelligence and Data Safety flows. [Storefront omission](https://shopify.dev/docs/api/storefront/latest/input-objects/VisitorConsent) means no expressed preference, not refusal. Do not inject inferred values. If the selected model requires Android collection, plan that as a separate source change.

P3-16 remains **NOT STARTED**. Production identity, signing, Play ownership/declarations, service bindings, public App Links, reviewer access, accountable deletion/support operators, restricted records, processor/retention and notice governance remain separate release inputs. The synthetic provider's 2026-10-05 schedule is not an additional entry condition.
