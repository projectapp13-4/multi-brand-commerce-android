query CartLinesPage($id: ID!, $after: String!) {
  cart(id: $id) {
    lines(first: 250, after: $after) {
      nodes {
        ...CartLineFields
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}
