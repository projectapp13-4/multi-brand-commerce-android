package com.gurbakir.mobile.catalog

import com.gurbakir.mobile.BuildConfig

internal object GurbakirCatalogConfiguration {
    val value =
        CatalogConfiguration(
            menuHandle = BuildConfig.CATALOG_MENU_HANDLE
        )
}
